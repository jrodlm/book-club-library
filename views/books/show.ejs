<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/show.css" />
    <title>
        <%= book.title %>
    </title>
    <link rel="stylesheet" href="/styles.css" />
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
    <div class="external-background-image">
        <h1>
                <%= book.title %> by <%= book.author %>
            </h1>
            <div class="summary-container">
                <p><b>Summary</b></p>
                <p>
                    <%= book.summary %>
                </p>
                <br>
                <p><b>Discussion Date</b> <%= new Date(book.discussionDate).toLocaleDateString('en-US') %></p>

                <a href="/books">Back To Your Club's Books</a>
                <% if (user.role==='admin' ) { %> <br>
            </div>
            <br>
            <div class="form-container">
                <a href="/books/<%= book._id %>/edit"><button type="button">Edit Book</button></a>
                <br>
                <form action="/books/<%= book._id %>?_method=DELETE" method="POST">
                    <button type="submit">Delete Book</button>
                </form>
                <% } else { %>
                    <a href="/books">Back To Your Club's Books</a>
                    <% } %>
                        </form>
            </div>
</body>

</html>